---
name: server_shutdown
pack: dpi_automation_pack
description: Triggers the shell script that will initiate the server shutdown
runner_type: remote-shell-script
entry_point: artifacts/shutdown.sh
enabled: true
parameters:
  servername:
    required: true
    type: string
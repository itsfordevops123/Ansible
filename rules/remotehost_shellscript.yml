---
    name: "remotehost_shellscript"                      # required
    pack: "remotehost_shellscript"                       # optional
    description: "This Rule is used to execute a remote shell command."       # optional
    enabled: true                          # required
    name: shell-script
    runner_type: "remote-shell-script"
    parameters:
       hosts:
         default: lx40392.starbucks.net
         required: true
         type: string
       cmd:
          "default: /opt/gsit/haws-restart-all-servers.sh"
          required: true
          type: string
          description: "Script to restart all servers "
          immutable: true
